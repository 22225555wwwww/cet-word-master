apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cet-word
resources:
  - namespace.yaml
  - secret.yaml # 由 secret.example.yaml 复制并修改，勿提交真实密码
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml